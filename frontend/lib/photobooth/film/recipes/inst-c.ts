import type { FilmRecipe } from '../types';
import instCJson from './inst-c.json';

/** Final Inst C filter — Vintage Polaroid / dreamy shoebox aesthetic. */
export const INST_C_RECIPE: FilmRecipe = instCJson as FilmRecipe;

export const INST_C_RECIPE_ID = INST_C_RECIPE.id;
